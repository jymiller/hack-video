# Pinned to what is verified working as of 29 July 2026.
# strands-agents shipped two days before the event from a repo that recently
# moved, so a floating install on the morning is avoidable risk.
# Regenerate with: .venv/bin/python -m pip freeze  (or re-run this block)
fastapi==0.140.13
uvicorn==0.51.0
httpx==0.28.1
python-multipart==0.0.32
python-dotenv==1.2.2
neo4j==6.2.0
twelvelabs==1.3.1
strands-agents[openai]==1.50.2
openai==2.50.0
